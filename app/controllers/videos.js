import Ember from 'ember';

export default Ember.Controller.extend({
  // queryParams: ["page"],
  //
  // perPage: Ember.computed.alias("limit"),
  // total_pages: Ember.computed.alias("num_pages"),

});
